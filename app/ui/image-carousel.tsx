import type { Image } from 'services/cloudinary-client/resources'

export default function ImageCarousel ({ index, images }: { index: number, images: Image[] }) {
  return <p>Images <code>len</code> goes here...</p>
}
